var React = require('react');

var EventForm = React.createClass({

  render: function() {
    return(
      <div>
        I'm the event form.
      </div>
    );
  }
});

module.exports = EventForm;
