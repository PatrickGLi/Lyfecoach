var React = require('react');

var Title = React.createClass({

  render: function() {
    return (
      <div>This is the title. Later it will change based on search.</div>
    );
  }

});

module.exports = Title;
